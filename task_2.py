
states = ['Отсутствует', 'Цветение', 'Зеленый', 'Красный']
index = 0

class Tomato:
  def __init__(self, index_0, states):
    self.states = states[0]
    self.index = index_0

  def grow(self, states):
    for i in range(states):
      i += 1
      states[i] 
      break
    return states

  def is_ripe(self, states):
    if states == 'red'
      print('созрел')

class TomatoBush:
  def __init__(self, tomatos_0): 
    self.tomatos = Tomato

  def grow_all(self, Tomato):
    a = Tomato(index, states)
    a.grow(self, states)

  def all_are_ripe(self, ):
    if == 'красный'
      spel == true
      return spel

  def give_away_all(self, ):


class Gardener:
  def __init__(self, name_0, plant):
    self.name = name_0
    self.plant = TomatoBush
  